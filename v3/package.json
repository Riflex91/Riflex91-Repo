{
  "name": "adventure-land-aio-v3",
  "version": "3.0.0-alpha.8.2",
  "private": true,
  "description": "Adventure Land AiO Bot v3 alpha: scheduler-owned farmer FSM with bounded combat risk, emergency disengage, and basic capability-based kiting.",
  "scripts": {
    "build": "node scripts/build.js",
    "test": "node --test test/*.test.js",
    "check": "npm run build && npm test && node --check dist/aio-v3.js && node scripts/smoke-bundle.js"
  },
  "engines": {
    "node": ">=20"
  }
}
