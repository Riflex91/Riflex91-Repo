{
  "name": "adventure-land-aio-v3",
  "version": "3.0.0-alpha.9.0",
  "private": true,
  "description": "Adventure Land AiO Bot v3 alpha: stability-first same-map local farming autonomy on top of the Alpha.8 combat, recovery, headless and persistence foundation.",
  "scripts": {
    "build": "node scripts/build.js",
    "test": "node --test test/*.test.js",
    "check": "npm run build && npm test && node --check dist/aio-v3.js && node scripts/smoke-bundle.js"
  },
  "engines": {
    "node": ">=20"
  }
}
