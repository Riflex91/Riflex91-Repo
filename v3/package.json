{
  "name": "adventure-land-aio-v3",
  "version": "3.0.0-alpha.8.20",
  "private": true,
  "description": "Adventure Land AiO Bot v3 alpha: stability-first combat foundation with verified command outcomes, bounded movement recovery, resilient persistence, knowledge aging, unknown-content quarantine and headless operations.",
  "scripts": {
    "build": "node scripts/build.js",
    "test": "node --test test/*.test.js",
    "check": "npm run build && npm test && node --check dist/aio-v3.js && node scripts/smoke-bundle.js"
  },
  "engines": {
    "node": ">=20"
  }
}
