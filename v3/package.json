{
  "name": "adventure-land-aio-v3",
  "version": "3.0.0-alpha.9.10",
  "private": true,
  "description": "Adventure Land AiO Bot v3 alpha: stability-first same-map autonomous farming with scheduler-owned navigation, strategic 32-24-5 Brain learning, bounded governance, resilient persistence, unknown-content quarantine and headless operations.",
  "scripts": {
    "build": "node scripts/build.js",
    "test": "node --test test/*.test.js",
    "check": "npm run build && npm test && node --check dist/aio-v3.js && node scripts/smoke-bundle.js"
  },
  "engines": {
    "node": ">=20"
  }
}
