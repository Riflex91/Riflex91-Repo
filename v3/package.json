{
  "name": "adventure-land-aio-v3",
  "version": "3.0.0-alpha.14.0",
  "private": true,
  "description": "Adventure Land AiO Bot v3 alpha: sustainable Inventory Ledger and Gear Progression planning on top of the supervised content-safe party foundation.",
  "scripts": {
    "build": "node scripts/build.js",
    "test": "node --test test/*.test.js",
    "check": "npm run build && npm test && node --check dist/aio-v3.js && node scripts/smoke-bundle.js"
  },
  "engines": { "node": ">=20" }
}
