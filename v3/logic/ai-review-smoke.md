# AI Logic Review Smoke Test

This file exists only to trigger the v3 pull-request review workflows and verify that the repository secret `OPENAI_API_KEY` is available to the read-only Codex reviewer.

No runtime, safety, release, build, or bot behavior is changed by this smoke test.
