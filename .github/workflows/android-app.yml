name: android-app

on:
  pull_request:
    branches: ["main", "feat/android-login-form"]
    paths:
      - "android-app/**"
      - "smartphone-clientless/**"
      - ".github/workflows/android-app.yml"
  push:
    branches: ["main", "feature/android-**", "feat/smartphone-clientless"]
    paths:
      - "android-app/**"
      - "smartphone-clientless/**"
      - ".github/workflows/android-app.yml"

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
      - uses: android-actions/setup-android@v3
        with:
          packages: platform-tools
      - name: Install Android SDK
        run: sdkmanager "platforms;android-35" "build-tools;35.0.0"
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.10.2"
      - name: Build, lint and unit tests
        working-directory: android-app
        run: gradle :app:lintDebug :app:testDebugUnitTest :app:assembleDebug --stacktrace
      - uses: actions/upload-artifact@v4
        with:
          name: aio-smartphone-clientless-debug-apk
          path: android-app/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
