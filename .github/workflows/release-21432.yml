name: release-21432
on:
  push:
    branches: ["release/2.14.32-transactional-liveness-dashboard"]
permissions:
  contents: read
jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "v2.14.32 release workflow archived; newer releases own main PR verification."
