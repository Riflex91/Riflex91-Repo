import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";
import {ProduktionsCdpBankItemTransferAdapter} from "../../werkzeuge/bank-item-transfer-produktions-write-browser.mjs";
test("Write-Adapter bindet beide offiziellen Public Functions und nie raw socket",()=>{const s=fs.readFileSync("werkzeuge/bank-item-transfer-produktions-write-browser.mjs","utf8");assert.match(s,/runner\.bank_retrieve\(E\.pack,E\.bankSlot,E\.inventorySlot\)/);assert.match(s,/runner\.bank_store\(E\.inventorySlot,E\.pack,E\.bankSlot\)/);assert.equal(/\.emit\s*\(/.test(s),false)});
test("Adapter erlaubt pro Instanz maximal einen Aufruf",async()=>{const session={async evaluate(){return {status:"BLOCKIERT"}}};const a=new ProduktionsCdpBankItemTransferAdapter("RETRIEVE",session,1);const req={modus:"RETRIEVE",pack:"items0",bankSlot:0,inventorySlot:0,accountId:"a",characterId:"c",sessionId:"s",serverRegion:"EU",serverIdentifier:"I",erwartetesItemName:"helmet",erwartetesItemFingerprint:"a".repeat(64),erwarteterPackRestFingerprint:"b".repeat(64),erwarteterInventoryRestFingerprint:"c".repeat(64),erwartetesCharacterGold:0,erwartetesBankGold:0,erwarteterFingerprint:"d".repeat(64)};assert.equal((await a.sende({},req)).art,"NICHT_GESENDET");await assert.rejects(()=>a.sende({},req),/MEHR_ALS_EIN_ADAPTER_AUFRUF/)});
