# CAP-033/034 Live-Abnahme – 2026-09-20

**Status:** VOLLSTAENDIG LIVE ABGENOMMEN  
**Datum:** 2026-09-20  
**Erster Live-Test auf Basis-`main`:** `e7e25bea528614cdfb615d6c8df10471f4776db9`  
**Evidence-Aktualisierung auf Basis-`main`:** `baae863da859d5d670289aef4da66ed7b1cebb40`  
**Harness:** `v5/werkzeuge/cap033-cap034-live-test-paket.js`  
**Testkennung:** `cap033-cap034-live-function-test`  
**GUI-Version:** `1.1.0`

## Scope

Diese Evidence dokumentiert die echte Adventure-Land-Funktionsabnahme von:

- CAP-033 Gear Progression / Self Gear im write-freien Live-Shadow;
- CAP-034 Upgrade mit serverseitigem Preview und exakt einem Controlled-Live-Send;
- CAP-034 Compound mit serverseitigem Preview und exakt einem Controlled-Live-Send;
- Durable Intent, One-Shot-Grenze und Postcondition-Verifikation beider real ausgeführten Mutationspfade.

Damit sind der normale Upgrade- und der normale Compound-Pfad jeweils mit echter Live-Mutation und beobachteter finaler Inventory-/Consumable-Postcondition nachgewiesen.

## 1. CAP-033/034 Live-Shadow

Charakter:

`My_Merchant`

Ergebnis:

- Status `BESTANDEN`;
- Dauer `300002 ms`;
- `21` Samples;
- `0` Sample-Gaps;
- Evidence-Kette gültig;
- CAP-033-Gear-Kandidat in `21/21` Samples;
- CAP-034-Mutationskandidat in `21/21` Samples;
- keine aktive V3- oder V4-Runtime;
- `q.upgrade = null`;
- `q.compound = null`;
- Performance-Trick aktiv und verifiziert;
- `0` Gameplay-Writes durch den Harness;
- `0` unerwartete Gameplay-Writes;
- keine Blocker.

Damit ist die reale Live-Evidence für Kandidatenerkennung, Runtime-Abwesenheit, Freshness-Kette und write-freie Shadow-Beobachtung erbracht.

## 2. CAP-034 Upgrade Preview

Kandidat:

- Ziel `poker +0`;
- Inventarindex `1`;
- Basiswert `16000` Gold;
- Ziel-Fingerprint `db15fcd3`;
- `scroll0`, Index `20`, Menge `2`;
- Kandidaten-Fingerprint `77cc0dd7`.

Server-Preview:

- Status `BESTANDEN`;
- `calculate: true`;
- Chance `0.9999999`;
- Harness-Mindestgrenze `0.99`;
- Response `upgrade_chance`;
- kein Verbrauch;
- `0` Gameplay-Writes;
- keine Blocker.

Die Preview wurde ausschließlich als Planning Evidence verwendet und nicht als Execution Authority.

## 3. CAP-034 Upgrade Controlled Live

Run-ID:

`CAP034-UPGRADE-1789922219046`

Verträge:

- Action Contract: `AL-ACTION-UPGRADE`;
- Recovery Contract: `AL-RECOVERY-UPGRADE`;
- Verifier: `AL-VERIFIER-UPGRADE`.

Sicherheitsgrenzen:

- maximal `1` Gameplay-Write;
- `sameIntentRetry=false`;
- keine unerwarteten Gameplay-Writes;
- frischer Preflight;
- Kandidaten-Fingerprint gegen Preview gebunden;
- frischer serverseitiger Preview direkt vor Send;
- Intent vor Mutation persistent journalisiert.

Server-Ergebnis:

- `success: true`;
- `level: 1`;
- `num: 1`;
- kein Serverfehler.

Postcondition:

- Klassifikation `BESTAETIGT_ERFOLG`;
- Zielitem danach `poker +1`;
- Ziel-Fingerprint danach `ca77e484`;
- Scrollmenge vorher `2`;
- Scrollmenge nachher `1`.

Damit ist exakt ein kontrollierter echter Upgrade-Send mit beobachteter und verifizierter Postcondition nachgewiesen.

## 4. CAP-034 Compound Vorprüfung und Preview

Der zweite reale Testlauf fand ebenfalls auf `My_Merchant` statt.

Vorprüfung:

- Status `BESTANDEN`;
- keine aktive V3- oder V4-Runtime;
- `q.upgrade = null`;
- `q.compound = null`;
- Performance-Trick aktiv und verifiziert;
- keine Blocker;
- `0` Gameplay-Writes.

Compound-Kandidat:

- `ringsj +0`, Index `15`, Fingerprint `e354c5c8`;
- `ringsj +0`, Index `18`, Fingerprint `c31d713b`;
- `ringsj +0`, Index `19`, Fingerprint `b106a33c`;
- jedes Ziel unlocked, plain, compound-faehig;
- Basiswert je Ziel `24000` Gold;
- `cscroll0`, Index `14`, Menge `1`;
- Gesamt-Kandidaten-Fingerprint `b96527b4`.

Server-Preview:

- Status `BESTANDEN`;
- `calculate: true`;
- Chance `0.99`;
- Harness-Mindestgrenze `0.99`;
- Response `compound_chance`;
- kein Verbrauch;
- `0` Gameplay-Writes;
- keine Blocker.

## 5. CAP-034 Compound Controlled Live

Run-ID:

`CAP034-COMPOUND-1789922525752`

Verträge:

- Action Contract: `AL-ACTION-COMPOUND`;
- Recovery Contract: `AL-RECOVERY-COMPOUND`;
- Verifier: `AL-VERIFIER-COMPOUND`.

Sicherheitsgrenzen:

- maximal `1` Gameplay-Write;
- `sameIntentRetry=false`;
- keine unerwarteten Gameplay-Writes;
- frischer Preflight;
- Kandidaten-Fingerprint gegen Preview gebunden;
- frischer serverseitiger Preview direkt vor Send;
- Intent vor Mutation persistent journalisiert.

Server-Ergebnis:

- `success: true`;
- `level: 1`;
- `num: 15`;
- kein Serverfehler.

Postcondition:

- Klassifikation `BESTAETIGT_ERFOLG`;
- Index `15`: `ringsj +1`, Fingerprint `6cc06b57`;
- Index `18`: `null`;
- Index `19`: `null`;
- Scrollmenge vorher `1`;
- Scrollmenge nachher `0`.

Damit ist die erwartete Compound-Erfolgssemantik live nachgewiesen: drei gleiche `+0`-Inputs wurden zu genau einem `+1`-Output konsolidiert, die beiden weiteren Inputs verschwanden und der Compound-Scroll wurde verbraucht.

## Ergebnis nach Capability

### CAP-033

**Live-Shadow bestanden.**

Nachgewiesen:

- reale physische Item-Evidence;
- stabiler Gear-Kandidat über den vollständigen 5-Minuten-Lauf;
- keine Fremdruntime;
- keine Gameplay-Mutation durch den Shadow-Harness.

Die CAP-033-Planung selbst erhält dadurch weiterhin keine Gameplay-/Execution-/Raw-Write-Authority.

### CAP-034 Upgrade

**Controlled Live bestanden.**

Der reale normale Upgrade-Pfad wurde von Preview über Durable Intent und genau einen Send bis zur finalen Inventar-/Consumable-Postcondition erfolgreich nachgewiesen.

### CAP-034 Compound

**Controlled Live bestanden.**

Der reale normale Compound-Pfad wurde von Preview über Durable Intent und genau einen Send bis zur finalen Inventar-/Consumable-Postcondition erfolgreich nachgewiesen.

## Abnahmefazit

CAP-033 und CAP-034 besitzen damit die geplante echte Adventure-Land-Funktionsabnahme:

- CAP-033: realer write-freier Live-Shadow bestanden;
- CAP-034 Upgrade: realer Controlled-Live-One-Shot bestanden;
- CAP-034 Compound: realer Controlled-Live-One-Shot bestanden;
- keine unerwarteten Gameplay-Writes;
- `sameIntentRetry=false`;
- keine UNKNOWN-/Timeout-Situation;
- beide Mutationspfade mit `BESTAETIGT_ERFOLG` reconciled.

Diese Evidence ist keine Freigabe fuer unbeschraenkte Mutationen. Die bestehenden Planner-, Authority-, Admission-, Budget-, Freshness-, Lock-, Recovery- und Quarantine-Grenzen bleiben unveraendert maßgeblich.

## Weiteres Vorgehen

1. CAP-033/034 nicht weiter als offene Live-Test-Luecke behandeln.
2. Vor neuer Entwicklungsarbeit CAP-035, CAP-036 und CAP-037 gegen den jeweils aktuellen `main` pruefen.
3. Der zuletzt geprüfte Repo-Stand weist diese drei Capabilities weiterhin nur als `V5_NATIVE_FOUNDATION` aus:
   - CAP-035: `v5/grundlage/quelle/produktion/production-graph.ts`;
   - CAP-036: `v5/grundlage/quelle/produktion/production-intent.ts`;
   - CAP-037: `v5/grundlage/quelle/produktion/bank-katalog.ts`.
4. Windows Host Adapter bleibt geparkt.

## Safety-Invarianten

Durch diese Live-Abnahme werden keine bestehenden Grenzen abgeschwaecht:

- Capability-/Owner-Authority bleibt maßgeblich;
- Operator-Deny und Kill Switch bleiben absolut;
- Admission direkt vor Mutation bleibt Pflicht;
- Freshness, Locks/Fencing und Resource Gates bleiben Pflicht;
- Durable Intent vor value-changing mutation bleibt Pflicht;
- UNKNOWN/Reconciliation statt Blind-Retry bleibt Pflicht;
- Learning besitzt keine Gameplay-/Safety-Authority.
