{
  "name": "adventure-land-bot-v4",
  "version": "4.0.0-dev.0",
  "private": true,
  "type": "module",
  "description": "Adventure Land Bot v4 architecture baseline",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "check": "npm run typecheck && npm run check:architecture && npm test",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "check:architecture": "depcruise --config dependency-cruiser.cjs apps packages",
    "test": "tsx --test tests/**/*.test.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "dependency-cruiser": "^18.2.0",
    "tsx": "^4.20.0",
    "typescript": "^7.0.2"
  }
}
