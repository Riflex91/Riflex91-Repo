import { readFile, writeFile } from 'node:fs/promises';
import path from 'node:path';
import process from 'node:process';

const wurzel = process.cwd();
const zielPfad = path.join(wurzel, 'werkzeuge', 'block8-6-live-paket.js');
const guiPfad = path.join(wurzel, 'werkzeuge', 'adventure-land-test-gui.js');
const runnerPfad = path.join(wurzel, 'werkzeuge', 'block8-6-freigabestufen-live-test.js');
const evidenzPfad = path.join(wurzel, 'dokumentation', 'BLOCK-8-6-9-SCHATTEN-FREIGABE-NACHWEIS.json');

export const BLOCK86_LIVE_RELEASE_SHA = 'ca0dfee7685563c8b6003469300c8fd08777b053';
export const BLOCK86_LIVE_CANDIDATE_SHA256 = 'b5d39ac692157ec98c9c77cc7d4afca0b39a0b67abbabbcc31b863a6b0f77ea5';
export const BLOCK86_LIVE_CANDIDATE_BYTES = 396471;
export const BLOCK86_LIVE_CANDIDATE_URL = 'https://aio-bot-dashboard.hansijuergenlul.workers.dev/v4/releases/ca0dfee7685563c8b6003469300c8fd08777b053/aio-v4-runtime.js';
export const BLOCK86_LIVE_LAUF_KENNUNG = 'block8-6-schatten-1789822653521';
export const BLOCK86_LIVE_BESTAETIGUNG = 'BLOCK8-6-KONTROLLIERT-LIVE:block8-6-schatten-1789822653521';

function pruefeEvidenz(evidenz) {
  if (
    evidenz?.schemaVersion !== 1 ||
    evidenz?.releaseSha !== BLOCK86_LIVE_RELEASE_SHA ||
    evidenz?.candidateSha256 !== BLOCK86_LIVE_CANDIDATE_SHA256 ||
    evidenz?.candidateBytes !== BLOCK86_LIVE_CANDIDATE_BYTES ||
    evidenz?.nachweis?.stufe !== 'schatten' ||
    evidenz?.nachweis?.ergebnis !== 'bestanden' ||
    evidenz?.nachweis?.spielAktionAusgefuehrt !== false ||
    evidenz?.reportEvidence?.status !== 'PASS' ||
    evidenz?.schattenUebergabe?.laufKennung !== BLOCK86_LIVE_LAUF_KENNUNG ||
    evidenz?.schattenUebergabe?.aenderungsKennung !== 'git:' + BLOCK86_LIVE_RELEASE_SHA ||
    evidenz?.schattenUebergabe?.nachweis?.ergebnis !== 'bestanden'
  ) {
    throw new Error('Block-8.6-Livepaket verlangt den exakt kanonisierten bestandenen Schattennachweis.');
  }
  return evidenz.schattenUebergabe;
}

function kopf(uebergabe) {
  const basis = "/* GENERATED: V4 Block 8.6.9 kontrolliertes Live-Komplettpaket.\n * Quelle: block8-6-live-paket-bauen.mjs\n * Bindet den real bestandenen Schattennachweis und startet exakt den immutable Block-8.6-Candidate aktiv.\n * Der Launcher ruft keine Adventure-Land-Spielaktionsfunktion direkt auf; aktive Kommunikation bleibt auf Produktionsheartbeat plus einen Capability-One-Shot begrenzt.\n */\n(() => {\n  'use strict';\n\n  const belegteNamen = [\n    'AIO_V4_RUNTIME_CONFIG',\n    'AIO_V4_CAPABILITY_CONFIG',\n    'AIO_V4_BLOCK86_FREIGABE_CONFIG',\n    'V4ProduktionsLaufzeit',\n    'V4CapabilityLaufzeit',\n    'V4Block86Candidate',\n    'V4Block86FreigabeLiveTest',\n    'V4TestGui',\n    'V4Block86LiveLauncher'\n  ].filter((name) => globalThis[name] !== undefined);\n  if (belegteNamen.length > 0) {\n    throw new Error(\n      'Block-8.6-Livepaket verlangt einen frischen Codekontext; bereits vorhanden: ' +\n      belegteNamen.join(', ')\n    );\n  }\n\n  function setzeGlobal(name, wert) {\n    Object.defineProperty(globalThis, name, {\n      configurable: true,\n      enumerable: true,\n      writable: false,\n      value: wert\n    });\n  }\n\n  setzeGlobal('AIO_V4_RUNTIME_CONFIG', Object.freeze({\n    aktivFreigegeben: true,\n    ablaufKennung: 'block8-6-kontrolliert-live-block8-6-schatten-1789822653521',\n    vertrauensNamen: Object.freeze(['My_Ranger1', 'My_Ranger2']),\n    faehigkeiten: Object.freeze({\n      heilen: 0,\n      schaden: 1,\n      aggro: 0,\n      schutz: 0,\n      unterstuetzung: 0\n    })\n  }));\n  setzeGlobal('AIO_V4_CAPABILITY_CONFIG', Object.freeze({\n    aktivFreigegeben: true,\n    ablaufKennung: 'block8-6-kontrolliert-live-block8-6-schatten-1789822653521',\n    vertrauensNamen: Object.freeze(['My_Ranger1', 'My_Ranger2']),\n    koordinationsNamen: Object.freeze(['My_Ranger1', 'My_Ranger2']),\n    policyVorgaben: Object.freeze([])\n  }));\n  setzeGlobal('AIO_V4_BLOCK86_FREIGABE_CONFIG', Object.freeze({\n    aenderungsKennung: 'git:ca0dfee7685563c8b6003469300c8fd08777b053',\n    laufKennung: 'block8-6-schatten-1789822653521',\n    modus: 'live',\n    soakDauerMillisekunden: 600000,\n    sampleMillisekunden: 5000,\n    recoveryReplayVerified: true,\n    schattenUebergabe: Object.freeze(__UEBERGABE__)\n  }));\n})();\n";
  return basis.replace('__UEBERGABE__', JSON.stringify(uebergabe, null, 2));
}

function fuss(runnerSource) {
  const basis = "\n/* ===== BEGIN Block-8.6-kontrollierter-Live-Launcher ===== */\n(() => {\n  'use strict';\n\n  const API_NAME = 'V4Block86LiveLauncher';\n  const VERSION = '1.0.0';\n  const RELEASE_SHA = 'ca0dfee7685563c8b6003469300c8fd08777b053';\n  const CANDIDATE_SHA256 = 'b5d39ac692157ec98c9c77cc7d4afca0b39a0b67abbabbcc31b863a6b0f77ea5';\n  const CANDIDATE_BYTES = 396471;\n  const CANDIDATE_URL = 'https://aio-bot-dashboard.hansijuergenlul.workers.dev/v4/releases/ca0dfee7685563c8b6003469300c8fd08777b053/aio-v4-runtime.js';\n  const CANDIDATE_MARKER = 'Adventure Land AiO Bot V4 | generated | Block 8.6 capability release candidate';\n  const LAUF_KENNUNG = 'block8-6-schatten-1789822653521';\n  const BESTAETIGUNG = 'BLOCK8-6-KONTROLLIERT-LIVE:block8-6-schatten-1789822653521';\n  const VERTRAUENS_NAMEN = Object.freeze(['My_Ranger1', 'My_Ranger2']);\n  const RUNNER_SOURCE = \"__RUNNER__\";\n\n  const guiApi = globalThis.V4TestGui;\n  if (!guiApi || typeof guiApi.erstelleTest !== 'function') {\n    throw new Error('V4TestGui fehlt im Block-8.6-Livepaket.');\n  }\n\n  const test = guiApi.erstelleTest({\n    kennung: 'block8-6-live-' + LAUF_KENNUNG,\n    titel: 'V4 Block 8.6.9 · Kontrolliert live',\n    beschreibung:\n      'Startet exakt den immutable Candidate, verlangt bestaetigten Produktionsheartbeat und erlaubt danach genau einen Capability-One-Shot an den anderen vertrauten Ranger.'\n  });\n\n  const zustand = {\n    phase: 'laedt',\n    bereit: false,\n    fehler: null,\n    releaseSha: RELEASE_SHA,\n    candidateSha256: CANDIDATE_SHA256,\n    candidateBytes: CANDIDATE_BYTES,\n    candidateUrl: CANDIDATE_URL,\n    laufKennung: LAUF_KENNUNG,\n    geladenerSha256: null,\n    lokalerCharakter: null,\n    zielName: null,\n    heartbeatVersucheVorFreigabe: 0,\n    heartbeatErfolgeVorFreigabe: 0,\n    letzterBericht: null\n  };\n\n  function fehlerText(ursache) {\n    return ursache instanceof Error ? ursache.message : String(ursache);\n  }\n\n  async function berechneSha256(code) {\n    if (!globalThis.crypto?.subtle || typeof globalThis.TextEncoder !== 'function') {\n      throw new Error('Web-Crypto oder TextEncoder ist fuer die Candidate-Hashpruefung nicht verfuegbar.');\n    }\n    const bytes = new globalThis.TextEncoder().encode(code);\n    const digest = await globalThis.crypto.subtle.digest('SHA-256', bytes);\n    return Array.from(new Uint8Array(digest), (wert) => wert.toString(16).padStart(2, '0')).join('');\n  }\n\n  function schlafe(ms) {\n    return new Promise((resolve) => setTimeout(resolve, ms));\n  }\n\n  async function warteAufProduktionsheartbeat(runtime) {\n    const gestartetAm = Date.now();\n    while (Date.now() - gestartetAm < 10000) {\n      const status = runtime.status();\n      if (status.lebensnachweisSendeFehler > 0) {\n        throw new Error(\n          'Produktionsheartbeat meldet vor kontrolliert live einen Sendefehler: ' +\n          String(status.lebensnachweisLetzterFehler ?? 'unbekannt')\n        );\n      }\n      if (\n        status.lebensnachweisSendeVersuche >= 1 &&\n        status.lebensnachweisSendeErfolge >= 1 &&\n        status.lebensnachweisSendeOffen === 0\n      ) {\n        return status;\n      }\n      await schlafe(250);\n    }\n    const status = runtime.status();\n    throw new Error(\n      'Innerhalb von 10 Sekunden wurde kein bestaetigter Produktionsheartbeat erreicht: ' +\n      JSON.stringify({\n        versuche: status.lebensnachweisSendeVersuche,\n        erfolge: status.lebensnachweisSendeErfolge,\n        fehler: status.lebensnachweisSendeFehler,\n        offen: status.lebensnachweisSendeOffen,\n        letzterFehler: status.lebensnachweisLetzterFehler\n      })\n    );\n  }\n\n  function pruefeVorStart() {\n    const runtime = globalThis.V4ProduktionsLaufzeit;\n    const capability = globalThis.V4CapabilityLaufzeit;\n    const candidate = globalThis.V4Block86Candidate;\n    const runner = globalThis.V4Block86FreigabeLiveTest;\n    const fehler = [];\n\n    if (!candidate || candidate.version !== '1.0.0') fehler.push('V4Block86Candidate 1.0.0');\n    if (candidate?.spielAutoritaet !== false) fehler.push('Candidate spielAutoritaet=false');\n    if (candidate?.neustartAutoritaet !== false) fehler.push('Candidate neustartAutoritaet=false');\n    if (!runtime || runtime.version !== '1.1.5') fehler.push('Runtime 1.1.5');\n    if (!capability || capability.version !== '1.0.0') fehler.push('Capability 1.0.0');\n    if (!runner || runner.version !== '1.0.0') fehler.push('Runner 1.0.0');\n    if (runner?.modus !== 'live') fehler.push('Runner modus=live');\n    if (runner?.laufKennung !== LAUF_KENNUNG) fehler.push('Runner Laufbindung');\n    if (runner?.aenderungsKennung !== 'git:ca0dfee7685563c8b6003469300c8fd08777b053') fehler.push('Runner Candidate-Bindung');\n\n    const r = runtime?.status?.();\n    if (r?.aktivFreigegeben !== true) fehler.push('Runtime aktivFreigegeben=true');\n    if (r?.empfangInstalliert !== false) fehler.push('Runtime vor Start empfangInstalliert=false');\n    if (r?.lebensnachweisAutomatikAktiv !== false) fehler.push('Runtime vor Start Heartbeat=false');\n    if (r?.lebensnachweisSendeVersuche !== 0) fehler.push('Runtime vor Start Heartbeat-Versuche=0');\n\n    const c = capability?.status?.();\n    if (c?.aktivFreigegeben !== true) fehler.push('Capability aktivFreigegeben=true');\n    if (c?.remoteBeobachtungInstalliert !== false) fehler.push('Remote-Beobachtung vor Klick=false');\n    if (c?.capabilityEmpfangInstalliert !== false) fehler.push('Capability-Empfang vor Klick=false');\n    if (c?.senden?.versuche !== 0 || c?.senden?.erfolge !== 0 || c?.senden?.fehler !== 0) {\n      fehler.push('Capability vor Klick Sendezaehler=0');\n    }\n\n    if (fehler.length > 0) {\n      throw new Error('Block-8.6-Live-Preflight vor Runtime-Start fehlgeschlagen: ' + fehler.join(', '));\n    }\n  }\n\n  async function kontrolliertLive() {\n    const runner = globalThis.V4Block86FreigabeLiveTest;\n    const runtime = globalThis.V4ProduktionsLaufzeit;\n    const capability = globalThis.V4CapabilityLaufzeit;\n\n    const vor = capability.status();\n    if (vor.senden.versuche !== 0 || vor.senden.erfolge !== 0 || vor.senden.fehler !== 0) {\n      throw new Error('Vor dem One-Shot sind Capability-Sendezaehler nicht mehr 0.');\n    }\n\n    const bericht = await runner.kontrolliertLive(BESTAETIGUNG);\n    const nachRuntime = runtime.status();\n    const nachCap = capability.status();\n\n    const erwartetesZiel = zustand.zielName;\n    const pass =\n      bericht?.pass === true &&\n      bericht?.stufe === 'kontrolliert_live' &&\n      bericht?.nachweis?.ergebnis === 'bestanden' &&\n      bericht?.nachweis?.begrenzt === true &&\n      bericht?.liveUebergabe?.aenderungsKennung === 'git:ca0dfee7685563c8b6003469300c8fd08777b053' &&\n      Array.isArray(bericht?.ziele) &&\n      bericht.ziele.length === 1 &&\n      bericht.ziele[0] === erwartetesZiel &&\n      nachCap.senden.versuche === 1 &&\n      nachCap.senden.erfolge === 1 &&\n      nachCap.senden.fehler === 0 &&\n      nachCap.remoteBeobachtungInstalliert === true &&\n      nachCap.capabilityEmpfangInstalliert === true &&\n      nachRuntime.lebensnachweisSendeErfolge >= 1 &&\n      nachRuntime.lebensnachweisSendeFehler === 0;\n\n    const gesamtbericht = Object.freeze({\n      schemaVersion: 1,\n      paketVersion: VERSION,\n      releaseSha: RELEASE_SHA,\n      candidateUrl: CANDIDATE_URL,\n      candidateSha256: CANDIDATE_SHA256,\n      candidateBytes: CANDIDATE_BYTES,\n      geladenerSha256: zustand.geladenerSha256,\n      laufKennung: LAUF_KENNUNG,\n      lokalerCharakter: zustand.lokalerCharakter,\n      zielName: erwartetesZiel,\n      pass,\n      runnerBericht: bericht,\n      runtimeStatus: nachRuntime,\n      capabilityStatus: nachCap\n    });\n    zustand.letzterBericht = gesamtbericht;\n\n    test.setzeErgebnis(\n      gesamtbericht,\n      pass ? 'pass' : 'fail',\n      pass\n        ? 'Block-8.6 kontrolliert live bestanden: genau 1 Capability-One-Shot an ' + String(erwartetesZiel) + ', 0 Capability-Sendefehler.'\n        : 'Block-8.6 kontrolliert live fehlgeschlagen. Gesamtbericht kopieren und nicht mit Soak fortfahren.'\n    );\n    test.protokolliere('Kontrolliert-live-Abschluss', gesamtbericht);\n    if (!pass) throw new Error('Kontrollierter Live-Bericht erfuellt die strikten PASS-Bedingungen nicht.');\n    return gesamtbericht;\n  }\n\n  test.registriereAktion({\n    kennung: 'kontrolliert-live',\n    titel: '2 · Kontrolliert live',\n    art: 'primaer',\n    aktiviert: false,\n    einmalig: true,\n    bestaetigungsText: BESTAETIGUNG,\n    ausfuehren: kontrolliertLive\n  });\n\n  Object.defineProperty(globalThis, API_NAME, {\n    configurable: true,\n    enumerable: true,\n    writable: false,\n    value: Object.freeze({\n      version: VERSION,\n      releaseSha: RELEASE_SHA,\n      candidateUrl: CANDIDATE_URL,\n      candidateSha256: CANDIDATE_SHA256,\n      candidateBytes: CANDIDATE_BYTES,\n      laufKennung: LAUF_KENNUNG,\n      bestaetigungsText: BESTAETIGUNG,\n      status() {\n        return Object.freeze({ ...zustand });\n      },\n      kopiereBericht: () => test.kopiereBericht()\n    })\n  });\n\n  test.setzeStatus(\n    'laeuft',\n    'Immutable Block-8.6-Candidate wird geladen. Kontrolliert live bleibt bis zu einem bestaetigten Produktionsheartbeat gesperrt.'\n  );\n\n  void (async () => {\n    try {\n      if (typeof globalThis.fetch !== 'function') {\n        throw new Error('fetch ist im Adventure-Land-Codekontext nicht verfuegbar.');\n      }\n      const response = await globalThis.fetch(CANDIDATE_URL, { cache: 'no-store' });\n      if (!response || response.ok !== true) {\n        throw new Error('Candidate-Download fehlgeschlagen: HTTP ' + String(response?.status ?? 'unbekannt') + '.');\n      }\n      const candidateCode = String(await response.text());\n      const bytes = new globalThis.TextEncoder().encode(candidateCode).byteLength;\n      if (bytes !== CANDIDATE_BYTES) {\n        throw new Error('Candidate-Bytegroesse stimmt nicht: ' + String(bytes) + '.');\n      }\n      if (!candidateCode.includes(CANDIDATE_MARKER)) {\n        throw new Error('Geladene Datei besitzt nicht den erwarteten Block-8.6-Candidate-Marker.');\n      }\n      const hash = await berechneSha256(candidateCode);\n      if (hash !== CANDIDATE_SHA256) {\n        throw new Error('Candidate-SHA-256 stimmt nicht: ' + hash + '.');\n      }\n      zustand.geladenerSha256 = hash;\n\n      (0, eval)(candidateCode);\n      (0, eval)(RUNNER_SOURCE);\n      pruefeVorStart();\n\n      const runtime = globalThis.V4ProduktionsLaufzeit;\n      runtime.starte();\n      const heartbeatStatus = await warteAufProduktionsheartbeat(runtime);\n\n      const capability = globalThis.V4CapabilityLaufzeit;\n      const update = capability.aktualisiere();\n      const capStatus = update.status;\n      const localName = capStatus.capabilityStatus?.charakterName;\n      if (!VERTRAUENS_NAMEN.includes(localName)) {\n        throw new Error(\n          'Kontrolliert live muss auf My_Ranger1 oder My_Ranger2 laufen; erkannt: ' + String(localName ?? 'unbekannt') + '.'\n        );\n      }\n      const ziel = VERTRAUENS_NAMEN.filter((name) => name !== localName);\n      if (ziel.length !== 1) {\n        throw new Error('Kontrolliert live konnte nicht exakt einen anderen vertrauten Ranger bestimmen.');\n      }\n      if (!capStatus.audit?.produktionsbereit || update.lokalerSnapshot === null) {\n        throw new Error('Capability-Audit oder lokaler Snapshot ist vor kontrolliert live nicht produktionsbereit.');\n      }\n      if (\n        capStatus.remoteBeobachtungInstalliert !== false ||\n        capStatus.capabilityEmpfangInstalliert !== false ||\n        capStatus.senden.versuche !== 0 ||\n        capStatus.senden.erfolge !== 0 ||\n        capStatus.senden.fehler !== 0\n      ) {\n        throw new Error('Capability-Laufzeit muss vor dem bestaetigten One-Shot komplett sendefrei sein.');\n      }\n\n      zustand.lokalerCharakter = localName;\n      zustand.zielName = ziel[0];\n      zustand.heartbeatVersucheVorFreigabe = heartbeatStatus.lebensnachweisSendeVersuche;\n      zustand.heartbeatErfolgeVorFreigabe = heartbeatStatus.lebensnachweisSendeErfolge;\n      zustand.phase = 'bereit';\n      zustand.bereit = true;\n\n      test.protokolliere('Block-8.6-Live-Launcher bereit', Object.freeze({\n        releaseSha: RELEASE_SHA,\n        candidateSha256: hash,\n        candidateBytes: bytes,\n        laufKennung: LAUF_KENNUNG,\n        lokalerCharakter: localName,\n        zielName: ziel[0],\n        runtimeVersion: runtime.version,\n        capabilityVersion: capability.version,\n        heartbeatVersuche: heartbeatStatus.lebensnachweisSendeVersuche,\n        heartbeatErfolge: heartbeatStatus.lebensnachweisSendeErfolge,\n        heartbeatFehler: heartbeatStatus.lebensnachweisSendeFehler,\n        capabilitySendeVersuche: capStatus.senden.versuche,\n        schattenNachweisKennung:\n          globalThis.AIO_V4_BLOCK86_FREIGABE_CONFIG.schattenUebergabe.nachweis.nachweisKennung\n      }));\n      test.setzeStatus(\n        'bereit',\n        'Live-Preflight bestanden: echter Produktionsheartbeat bestaetigt, Schattenuebergabe gebunden, Ziel ' +\n        String(ziel[0]) +\n        '. Jetzt nur „2 · Kontrolliert live“ mit dem angezeigten Bestaetigungstext ausfuehren.'\n      );\n      test.setzeAktionAktiv('kontrolliert-live', true);\n    } catch (ursache) {\n      const meldung = fehlerText(ursache);\n      zustand.phase = 'fehlgeschlagen';\n      zustand.bereit = false;\n      zustand.fehler = meldung;\n      test.setzeAktionAktiv('kontrolliert-live', false);\n      test.setzeErgebnis(\n        Object.freeze({ status: 'fehlgeschlagen', fehler: meldung }),\n        'fail',\n        meldung\n      );\n      test.protokolliere('Block-8.6-Live-Launcher FEHLER', meldung);\n    }\n  })();\n})();\n/* ===== END Block-8.6-kontrollierter-Live-Launcher ===== */\n";
  return basis.replace(JSON.stringify('__RUNNER__'), JSON.stringify(runnerSource));
}

export async function baueBlock86LivePaket() {
  const [gui, runner, evidenzRoh] = await Promise.all([
    readFile(guiPfad, 'utf8'),
    readFile(runnerPfad, 'utf8'),
    readFile(evidenzPfad, 'utf8')
  ]);
  const uebergabe = pruefeEvidenz(JSON.parse(evidenzRoh));
  const paket = [
    kopf(uebergabe),
    '\n/* ===== BEGIN werkzeuge/adventure-land-test-gui.js ===== */\n',
    gui.trim(),
    '\n/* ===== END werkzeuge/adventure-land-test-gui.js ===== */\n',
    fuss(runner.trim())
  ].join('');
  return paket.endsWith('\n') ? paket : paket + '\n';
}

const pruefen = process.argv.includes('--pruefen');
const paket = await baueBlock86LivePaket();

if (pruefen) {
  const vorhanden = await readFile(zielPfad, 'utf8');
  if (vorhanden !== paket) {
    throw new Error(
      'block8-6-live-paket.js ist nicht source-locked. Mit npm run block8-6-live-paket:bauen neu erzeugen.'
    );
  }
  console.log('Block-8.6 Livepaket source-locked: ' + new TextEncoder().encode(paket).byteLength + ' Bytes.');
} else {
  await writeFile(zielPfad, paket, 'utf8');
  console.log(
    'Block-8.6 Livepaket gebaut: ' +
    path.relative(wurzel, zielPfad) +
    ' (' +
    new TextEncoder().encode(paket).byteLength +
    ' Bytes).'
  );
}
