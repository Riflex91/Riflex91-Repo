{
  "name": "adventure-land-aio-smartphone-clientless",
  "version": "0.1.0",
  "private": true,
  "description": "Browser-free Android runtime port of Adventure Land AiO v3",
  "scripts": {
    "test": "node --test test/*.test.js",
    "check": "node --check src/runtime/ClientlessGameAdapter.js && node --check src/runtime/ClientlessSession.js && node --check src/runtime/CharacterRuntimeManager.js && npm test"
  },
  "engines": { "node": ">=20" }
}
